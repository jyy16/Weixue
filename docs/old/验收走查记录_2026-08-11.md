# 验收走查记录（2026-08-11，离线批次）

> 走查人：A（zlx）· 依据：[端到端验收走查清单_v1.md](./端到端验收走查清单_v1.md) §0–§2、§7
> 方式：**临时 SQLite 库 + mock 转写 + 空 LLM key**（零额度、零真实库副作用），
> 后端子进程 :8100，全部经 HTTP API 实测；UI 层点击走查另列待办。

## 结果一览

| 清单条目 | 结果 | 证据 |
|---|---|---|
| §0 后端可启动 | ✅ | `/api/health` 返回 `database: ready`；飞书未配置如实报 `not_configured`（status=degraded，不伪造） |
| §0 前端可启动 | ✅ | vite dev :5173 与 demo 构建均正常（8/11 重建 dist） |
| §0 线上 demo 更新 | ⏳ | 已重建构建并提 PR（gh-pages 重建 PR #7），合并即生效 |
| §0 演示数据可恢复 | ✅ | seed→批改→export→restore 逐字节往返验证通过（8/10，临时库） |
| §0 30–60s 示例音频 | ✅ | `backend/data/sample_class_audio.wav`（45.05s，16k 单声道） |
| §1 辩题增删改排序 | ✅ | 新增→改名→成对交换 order 排到首位→删除，API 实测通过 |
| §1 学生单个/批量 + 交叉查看 | ✅ | 批量接口正确返回 created/skipped；学生↔辩题交叉查询 200 |
| §1 音频上传转写 | ✅ | mock 转写离线通过；**真实转写 8/11 实测通过**：45s 音频上传→qwen3-asr-flash 逐字转写→作答入库 `source=audio` |
| §1 粘贴文本 | ✅ | 作答入库 `source=manual` |
| §1 上传失败兜底 | ✅ | 错误格式 400 + 清晰文案（`unsupported audio type: .txt (allowed: ...)`）；前端分级提示 + 粘贴兜底已上线；失败上传后端零残留（test_asr.py 覆盖） |
| §2 批量评估 + 进度 | ✅ | reset→assess（total=11）→轮询进度→completed=11，无卡死 |
| §2 认知梯度维度 | ✅ | 三梯段 prompt 离线校验：基础层含清晰性/解释力/证据意识，发展层加相关性/因果推理/证据使用，进阶层含论证质量/深度与广度/反思调节，互不相同 |
| §2 AI 初稿（六级+推理链+标签） | ✅ | 8/11 真实 LLM 实测：certain_good + 六级评分 + note + 建议标签入库；离线降级行为亦已覆盖 |
| §2 教师改分/标签/批注 + 校准记录 | ✅ | review 200，校准列表出现 AI 初评→教师终评→理由 |
| §2 刷新持久化 | ✅ | 数据落 SQLite，GET 回读一致 |
| §2 raw/cleaned 可回看 | ✅ | 响应模型含两字段 |
| §7 LLM 不可用兜底 | ✅ | 无 key：11 条作答全部 `uncertain` + "AI评估失败"note（**不伪造已评**）；评语走模板回退正常产出草稿 |
| §7 断网纯前端 demo | ✅（构建级） | `VITE_DEMO_MODE=true` 构建成功；完整点击走查见待办 |
| §7 数据改坏一键恢复 | ✅ | restore_demo_state.py 往返验证（8/10） |

## 走查中发现并处理的问题

- 走查脚本自身三处断言错误（批量接口 body 形状、排序用成对交换、维度名"深度与广度"），
  均非产品 bug，修正后 15/15 全绿；
- 旧 `frontend/src/demo-data.json` 为 8/4 开发库导出（61 标签/13 校准残留），
  已按 seed→export 流程重新导出为规范快照（27 标签/4 校准）并提交。

## 待办（需要拍板或依赖外部）

1. ~~**额度类实测**（等拍板）：qwen_asr 真实音频端到端（任务 1）、9人×3题真实批量评估压测（任务 4）、§2 AI 初稿真实生成走查~~ —— **已于 8/11 完成**（见上表 ✅ 与 [音频录入与转写.md](./音频录入与转写.md) §9）；
2. **后端托管部署**：需要一个托管账号（Render/Railway 等），待定；
3. **UI 点击级走查**：§1–§5 页面交互（API 层已覆盖逻辑）；
4. gh-pages PR 合并后线上验证资源哈希。
