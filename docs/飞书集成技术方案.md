# 飞书集成技术方案（草稿 v0.1）

> 状态：规划中 · 待团队评审
> 关联：2026 AI先锋未来人才大赛 · 飞书命题

> **当前状态（2026-08-04）**：妙记 API 权限在比赛租户内暂不可用（报 99991679，接口要求用户身份授权），音频转写已切换为独立 ASR 抽象层并实现（见 [音频录入与转写.md](./音频录入与转写.md)）。飞书 AI 能力叙事待定：候选为多维表格 AI 字段捷径 / aily + 机器人推送。若妙记权限恢复，`feishu/minutes.py` 可作为 ASR 的另一个 provider 接回。

## 1. 背景与目标

### 1.1 为什么要接飞书

- **大赛硬性要求**：参赛作品应至少使用一项飞书 AI 产品能力。
- **评分维度"AI 应用深度"**：飞书能力需要长在产品工作流里，而不是最后外挂一个"机器人发消息"。
- **业务契合**：思辨课以口头表达为主，学生发言转写是教师最痛的环节；评语审阅与发放天然发生在聊天和表格场景。

### 1.2 集成目标

**核心目标（MVP）**：课堂音频 → 飞书妙记自动转写 → 进入现有双层评估流水线 → 评估结果沉淀到飞书多维表格 → 机器人推送评语卡片，教师一键确认。

**扩展目标**：教师在多维表格内完成审阅决策；@机器人 按指令生成/推送评语；多维表格 AI 字段捷径辅助打标签。

### 1.3 设计原则

1. 现有 FastAPI + SQLite + LLM 流水线保持为主干，飞书作为**输入层 / 展示层 / 交互层**接入。
2. **本地库是唯一事实源**，多维表格是同步展示与审阅面（8/13 起双向同步：推送 + 手动拉取教师编辑，详见 §4.2）。
3. 每个飞书能力可独立失败回退：妙记不可用时回退到手动录入文本，机器人不可用时评语仍在 Web 页面生成。

## 2. 总体架构

```mermaid
flowchart LR
    subgraph 输入
        A[课堂录音 / 音频文件]
    end

    subgraph 飞书
        M[妙记 · AI 语音转写]
        B[(多维表格 Bitable)]
        R[机器人 · IM 卡片]
    end

    subgraph 我们后端
        F[FastAPI 评估引擎]
        D[(SQLite 本地库)]
    end

    subgraph 教师
        T[飞书客户端 / Web 前端]
    end

    A -->|上传音频| M
    M -->|转写文本| F
    F -->|写入/更新| B
    F -->|评估结果+评语| R
    B -->|教师审阅/修改| T
    T -->|卡片按钮确认| R
    R -->|交互回调| F
    F --> D
```

**叙事主线**：课堂语音从飞书进来（妙记），评估结果从飞书出去（多维表格 + 机器人），教师在整个链路里做决定。

## 3. 飞书能力选型

| 能力 | 在系统中的角色 | 是否 AI 产品能力 | 价值点 |
|------|--------------|-----------------|--------|
| 妙记（语音转写） | 输入层 | ✅ 是（ASR） | 课堂口头发言自动转写，直接对接双层流水线 Layer 1 |
| 多维表格（+ AI 字段捷径） | 数据层 | 可选（AI 字段捷径是 AI 能力） | 评估数据沉淀、教师审阅面、AI 自动打标签 |
| 机器人（IM 消息/卡片） | 交互层 | 否（基础能力） | 评语推送、待审清单、一键确认 |
| aily 智能伙伴 / 智能体 | 扩展（可选） | ✅ 是 | 对话式评估助手，后期可加 |

**结论**：以"妙记 + 多维表格"作为作品中明确使用的飞书 AI 产品能力（妙记语音转写是纯 AI 能力，与双层流水线叙事无缝衔接），机器人作为交互载体。多维表格 AI 字段捷径可作为演示中的补充亮点。

## 4. 模块详细设计

### 4.0 鉴权与客户端（前置基建）

- 使用**大赛专属租户**开通的自建应用（企业自建应用），获取 App ID / App Secret。
- 统一封装 `backend/feishu/client.py`：
  - `tenant_access_token`：`POST https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal`，请求体 `{"app_id": ..., "app_secret": ...}`；有效期约 2 小时，内存缓存 + 到期前 5 分钟自动刷新。
  - MVP 阶段尽量只操作**应用身份可访问的资源**（应用所在租户的共享文件夹、机器人的可见范围），避免 `user_access_token` 的 OAuth 流程；确需用户资源时再引入授权码模式。
- 技术选型：官方 Python SDK `lark-oapi`（自带 token 管理、事件签名校验、卡片回调处理）；若 SDK 与当前 Python 3.14 兼容性有问题，回退为手写 httpx（仓库已有该依赖）。

### 4.1 模块 A：妙记转写（语音评估输入）

**目标**：教师上传课堂音频 → 自动转写 → 写入 `student_responses.raw_text` → 走现有评估流水线。

**数据流**：

1. 前端（或机器人指令）上传音频 → `POST /api/feishu/minutes/import`，参数 `{course_id, student_id, topic_id, file}`。
2. 后端两步上传：
   - `POST /open-apis/drive/v1/medias/upload_all` 上传媒体文件，得到 `file_token`；
   - 用 `file_token` 创建妙记（具体端点待验证，见 §7 待验证清单）。
3. 轮询妙记转写状态（若支持转写完成事件则改为事件订阅触发，待验证）。
4. 转写就绪后调用 **导出妙记文字记录**：`GET /open-apis/minutes/v1/minutes/{minute_id}/transcript`，获取转写文本。
5. 转写文本写入 `StudentResponse.raw_text`，`source="asr"`，记录 `feishu_minute_id`。
6. 触发现有 `evaluator.assess()`（Layer 1 清洗 + Layer 2 评估），与手动文本流程完全一致。

**约束**：

- 妙记语音转文字额度：**每个自然月每个用户累计 300 分钟**（含会议录制、录音、本地文件上传、云文件导入四种场景），超出的新妙记不再生成文字记录。演示前要核对当月用量。
- 支持格式 / 单文件时长上限：实施时验证（常见 mp3 / wav / m4a，建议单文件控制在 1 小时内）。
- 转写文本会保留口语填充词（"嗯""那个"）——这正好是 Layer 1 清洗的输入，叙事上自洽。

**API 变更**：新增 `POST /api/feishu/minutes/import`、`GET /api/feishu/minutes/{id}/status`；`StudentResponse` 增加 `feishu_minute_id`、`source` 字段。

### 4.2 模块 B：多维表格（数据沉淀 + 教师审阅面）

**目标**：评估数据同步到多维表格，教师能在表格里看到原始文本、清洗文本、AI 评分、教师评分、标签与状态。

**表结构建议（4 张表，与现有数据模型一一对应）**：

1. `courses` 班级表：班级名、年级、创建时间
2. `topics` 辩题表：标题、类型（单选）、认知梯段（单选）、引导材料、参考论据、顺序
3. `students` 学生表：姓名、年级（数字）、认知梯段（单选）、班级、评语草稿
4. `responses` 评估记录表：学生、辩题、班级（拉取时按课程过滤用，8/13 补）、来源（单选：手动/妙记）、原始文本、清洗文本、AI 评分摘要（文本）、各维度 AI 评分（每维度一列，单选 A+/A/A-/B+/B/B-）、AI 置信度（单选）、AI 建议标签（多选）、教师评分（文本）、教师标签（多选）、教师批注、状态（单选：待评估/AI 已评/教师已审）、更新时间

**同步策略（双向同步，本地库为主，8/13 落地，8/13 晚按代码审查修订）**：

- 推送（原有）：评估完成 / 教师保存后，后端调用批量接口写回多维表格。整课推送为**单事务**：每个实体用 savepoint 隔离，单实体失败只回滚自己，整课最后统一 commit，避免"半同步"与大量小事务。
- 拉取（新增）：`POST /api/feishu/bitable/pull`（手动触发，前端「从表格导入」按钮）。把教师在多维表格里拉动的**教师列**写回本地库：responses 表的教师评分 / 教师标签 / 教师批注 / 状态，students 表的评语草稿。
- 按课程过滤：拉取用 `search` 的 filter 按「班级=本班」取行，其它课程的行不会进入拉取逻辑，`unmatched_remote` 只统计**本班**真正无绑定的行。远端表若是建在「班级」字段补上之前（filter 报错），降级为全表扫描并回报 `filtered=false`，此时未匹配数不可靠、一律不计入。
- 字段所有权切分：后端只写内容与 AI 列（原始/清洗文本、AI 评分摘要、置信度、建议标签、加分项、来源、班级、更新时间），教师只写上述教师列；拉取只读教师列，双写字段天然错开，绝大多数情况无冲突。
- 「状态」的定位（审查明确）：它**不是**真正的双向字段，而是本地状态的**前向单向投影**——推送按 `teacher_reviewed` / AI 评分派生，拉取只把它向「教师已审」推进、绝不撤销批改；教师在表格里把状态改回去不会生效（下次推送按本地重新派生）。
- 防回环：`feishu_bindings` 新增 `last_synced_hash`，推送后对教师列做规范化哈希快照；拉取时远端哈希与快照一致即视为"自己推上去的回声"，跳过；不一致才回写。`(entity_type, entity_id, table_key)` 已加唯一约束（旧库启动时自动去重迁移），并发/历史重复不会产生重复绑定。
- 安全方向：远端「状态=教师已审」只会把本地推向已审，**绝不撤销**已有批改；远端评分摘要按"立意:A；选材:B+"格式解析回维度字典（未知维度名原样保留）。
- 删除策略（审查明确）：本地删除实体**不删远端行**——多维表格是教师的审阅面，数据安全优先，远端行保留；同步时自动剪除本地孤儿绑定（`pruned_bindings`），防止其被误计为未匹配行。远端删除也不联动删本地。
- 未匹配远端行（教师手加、本地无绑定）只计数报告（`unmatched_remote`），不自动创建本地记录。
- 旧绑定（双向功能上线前创建、无哈希基线）首次拉取只认基线不回写，防止空远端覆盖本地已有批改；其后的真实编辑正常生效。
- 事件订阅 / 定时轮询依赖公网回调或常驻任务，列为后续迭代（依赖后端托管）。

**关键 API**：

- 列出数据表：`GET /open-apis/bitable/v1/apps/{app_token}/tables`
- 查询记录：`POST /open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/records/search`（单次 ≤500 行，支持 filter 与分页）
- 新增多条记录：`POST .../records/batch_create`（单次 ≤1000 条）
- 更新多条记录：`POST .../records/batch_update`（按 `record_id` 幂等匹配）
- 字段写入格式：文本=1、数字=2、单选=3（选项需已存在）、多选=4、日期=5（毫秒时间戳）、人员=11（open_id）

**待验证**：`app_token` 的获取（wiki 链接需先调用知识空间节点接口解析）。字段管理接口（建字段/列字段/改选项）8/13 已按官方文档核对形状（type 编号 1/2/3/4/5/7/11、`property.options=[{name}]`、响应键 `data.field.field_id`、PUT 为全量覆盖且必带 `field_name`+`type`），代码已按此修正（ensure_schema 补齐 prep_plans 表、选项补齐改并集语义以保留教师自定义选项），**真机执行仍待联调**（`python -m feishu.schema --apply` 或 `python -m feishu.bootstrap_base`）。

### 4.3 模块 C：机器人（评语推送与交互）

**目标**：评估完成后机器人推送"待审清单"和"评语卡片"；教师点卡片按钮确认 / 修改，回调落库并生成校准记录。

**数据流**：

1. 批量评估完成 → `POST /open-apis/im/v1/messages?receive_id_type=open_id` 向教师发送评语卡片（`msg_type=interactive`，`content` 为卡片 JSON 字符串）。
2. 卡片含"确认评分 / 修改 / 发送给学生"按钮 → 飞书交互卡片回调 `POST` 到后端 `/api/feishu/card`。
3. 后端校验回调签名 → 更新 `teacher_reviewed`、教师评分 → 复用现有逻辑写校准记录。
4. 进阶：教师 @机器人 发送"给小雨生成评语" → 事件订阅 `im.message.receive_v1` → 机器人调用现有 `generate_comment` 逻辑回复。

**关键 API**：

- 发送消息：`POST /open-apis/im/v1/messages?receive_id_type=open_id`（支持 text / post / interactive）
- 回复消息：`POST /open-apis/im/v1/messages/{message_id}/reply`
- 事件订阅：配置回调 URL，需支持 `challenge` 验证与事件签名校验（Verification Token / Encrypt Key）
- 限频：消息发送整体 1000 次/分、50 次/秒；同一用户 5 QPS——批量推送需分批

**注意**：必须是**应用机器人**（开发者后台开启机器人能力），群自定义机器人无法调用 `im/v1/messages`。

> **实现状态（2026-08-10 晚，对本节设计的修订；2026-08-14 更新启动方式）**：回调通道没有走 HTTP 回调（无需公网回调地址、无需 challenge 验证），改用**飞书长连接**——`feishu/ws_listener.py` 常驻进程基于 lark-oapi `ws.Client` 接收 `card.action.trigger`（新版卡片平台的卡片回调经事件通道投递）与 `im.message.receive_v1`。HTTP `/api/feishu/card` 路由保留为备用通道，与 ws 共用分发入口 `feishu/card_actions.py`。控制台"订阅方式"须选"使用长连接接收事件"并发布新版本。
>
> **启动方式（2026-08-14 起）**：用 `python start.py` 一键启动后端时会自动以子进程拉起监听器（日志 `ws_listener.log`；检测到已有实例则不会重复启动），也可手动执行 `python -m feishu.ws_listener`（须在 backend 根目录）。启动姿势、验证方法与踩坑记录见《docs/old/多维表格联调记录_2026-08-10.md》Module C 章节。

### 4.4 后端代码落地

```
backend/feishu/
├── __init__.py
├── client.py           # token 缓存、HTTP 封装、错误处理、限频退避
├── minutes.py          # 音频上传、妙记创建、转写轮询、文本获取
├── bitable.py          # 表结构定义、记录批量读写、字段类型映射
├── schema.py           # 多维表格字段/选项幂等创建（--apply）
├── sync.py             # 本地 SQLite ⇄ 多维表格双向同步（推送 + 拉取教师编辑）
├── bootstrap_base.py   # 一键引导：建 base/表、token 写回 .env、端到端验证
├── bot.py              # 消息/卡片发送（schema 2.0 平铺按钮）
├── card_actions.py     # 卡片按钮回调统一分发（HTTP 与 ws 共用）
├── ws_listener.py      # 长连接常驻进程：卡片回调 + 机器人消息
├── reviews.py          # 教师审阅落库逻辑（网页与卡片回调共用）
├── resolve_open_id.py  # 按邮箱/手机号解析 open_id
├── check.py            # 飞书鉴权与妙记就绪检查
└── routes.py           # /api/feishu/* 路由挂载
```

**`.env` 新增配置**：

```dotenv
FEISHU_APP_ID=cli_xxx
FEISHU_APP_SECRET=xxx
FEISHU_BITABLE_APP_TOKEN=xxx
FEISHU_BITABLE_TABLE_IDS={"courses":"...","topics":"...","students":"...","responses":"..."}
FEISHU_VERIFICATION_TOKEN=xxx
FEISHU_ENCRYPT_KEY=xxx
FEISHU_TEACHER_OPEN_ID=ou_xxx
FEISHU_WEB_BASE_URL=http://127.0.0.1:8000   # 卡片跳转按钮的网页基址，默认即后端自身
```

长连接模式下 `FEISHU_VERIFICATION_TOKEN` / `FEISHU_ENCRYPT_KEY` 可留空（仅 HTTP 回调模式需要）。

**依赖**：`lark-oapi`（官方 Python SDK）。

### 4.5 音频转写抽象层（妙记不可用时的备选路线）

比赛租户内妙记 API 权限暂不可用（报 99991679，接口要求用户身份授权）。因此音频转写走独立的 ASR 抽象层（`backend/asr.py`），不依赖飞书。**该方案已实现**（2026-08-04），详见 [音频录入与转写.md](./音频录入与转写.md)：

- 前端上传音频 → `POST /api/courses/{cid}/audio/import` → 保存到 `uploads/` → ASR 转写 → 写入 `raw_text` → 重新评估。
- `ASR_PROVIDER`：`mock`（默认，演示保底，无需任何 API）/ `openai`（whisper 兼容）/ `dashscope`（百炼 paraformer，需 `pip install dashscope`）。
- 若妙记权限后续恢复，`feishu/minutes.py` 可作为 ASR 的另一个 provider 接回，前端无需改动。
- 飞书 AI 能力叙事改为：**多维表格 AI 字段捷径 / aily**（待实测可用性）+ 机器人推送不变。

## 5. 权限与开通清单（开放平台后台）

| 能力 | 权限（名称以最新后台为准，待验证） |
|------|----------------------------------|
| 机器人发消息 | 获取与发送单聊、群组消息（im:message）；以应用身份发送消息（im:message:send_as_bot） |
| 妙记 | 读取妙记；**导出妙记转写的文字内容**（"导出妙记文件"权限已不对新应用开放） |
| 多维表格 | 查看、编辑多维表格（bitable 相关文档权限） |
| 云盘媒体 | 上传、读取文件（drive:drive 相关权限） |
| 事件订阅 | im.message.receive_v1 等 |

## 6. 实施路线（里程碑）

| 里程碑 | 周期 | 负责人 | 交付物 |
|--------|------|--------|--------|
| M0 前置 | 半天 | 队长 | 确认大赛租户已开通、创建自建应用、拿到 App ID/Secret、向企业教练确认妙记权限与配额 |
| M1 基建 | 1 天 | 后端 | `feishu/client.py` + token 缓存 + health check 路由 + `.env` 配置 |
| M2 妙记闭环 | 2-3 天 | 后端（队长提供演示音频） | 上传示例音频 → 转写 → 评估 → 前端显示"妙记来源"标记 |
| M3 多维表格 | 2 天 | 后端 + 前端 | 建 4 张表、批量写入、前端新增"飞书同步"状态 |
| M4 机器人 | 2 天 | 后端 | 评语卡片推送、卡片按钮确认回调、事件订阅（或轮询兜底） |
| M5 演示打磨 | 2 天 | 全员 | 端到端 demo 脚本、示例音频、README/文档更新、部署 |

**风险与兜底**：

- 妙记 API 权限 / 额度不满足 → 兜底方案：录音文件手动上传 + 后端调用转写服务（削弱"飞书 AI 能力"叙事，尽量避免，先找教练确认）。
- 公网回调困难 → 机器人交互先用"主动推送 + 轮询查询"实现，事件订阅列为可选。
- 多维表格字段类型兼容问题 → 维度评分以文本摘要列兜底展示，不阻塞 MVP。

## 7. 待验证清单（写代码前确认）

- [x] 妙记接口要求用户身份授权（`minutes:minute:download` / `minutes:minutes.transcript:export`）——已确认，**妙记已搁置**
- [x] "导出妙记转写的文字内容"权限（`minutes:minutes.transcript:export`）——确认存在，但比赛租户无法申请/授权，已搁置
- [ ] 妙记创建 / 上传的准确端点与请求参数（drive `upload_all` → minutes 创建）——仅当妙记恢复时才需要
- [ ] 妙记转写完成是可订阅事件，还是需要轮询——同上
- [ ] 妙记支持的文件格式、大小、时长上限——同上
- [x] 多维表格单选字段选项是否可自动创建——字段管理 API（POST/PUT fields）支持带 options 创建/更新；已实现 `python -m feishu.schema --apply`（幂等建字段+选项），真实租户联调后验证
- [ ] 大赛专属租户对开放平台 API 的可用范围（是否与企业版一致）
- [x] 机器人卡片按钮回调（card action）的签名校验方式——官方 Python SDK `CardActionHandler`：`sha1(timestamp + nonce + verification_token + raw_body)` 的 hex 摘要，比对 `X-Lark-Signature`；事件订阅用 `sha256(timestamp + nonce + encrypt_key + raw_body)`（见 `backend/feishu/bot.py` 与 `backend/feishu/routes.py`）
- [ ] `lark-oapi` SDK 与仓库 Python 3.14 环境的兼容性

**ASR 兜底路线（妙记搁置后的实际转写通道，2026-08-11 更新）**：

- [x] 百炼 compatible-mode 无 `/audio/transcriptions`（whisper 风格端点 404）——已实测确认，openai 路线对百炼 key 不可用
- [x] `qwen3-asr-flash` 走 chat/completions + 音频 base64 data URL 的端点/模型/key 可用性——已用探测请求验证（空音频返回业务错误"The audio is empty"而非 404/鉴权错误）
- [x] 真实课堂录音 30–60s 端到端实测（上传→转写→评估→评语）——2026-08-11 实测通过：45 秒示例音频上传→qwen3-asr-flash 转写逐字正确→入库 `source=audio`→qwen-plus 真实评估（certain_good）→教师批改→真实评语生成
- [x] 音频预处理：m4a/webm/ogg → 16k 单声道 WAV（`backend/audio_utils.py`）——已验证

## 8. 演示叙事建议（供队长写脚本）

90 秒主线：

1. 课堂录音上传 → AI 转写（当前走百炼 qwen3-asr-flash；若妙记权限恢复则升级为妙记转写，展示"嗯""那个"等口语噪声）；
2. 双层流水线：清洗后文本 vs 原始文本对比展示；
3. 多维表格展示评估结果：各维度评分 + 推理链 + AI 建议标签；
4. 教师在页面/表格里修改 1 个评分 → 系统记录校准差异；
5. 机器人推送评语卡片 → 教师一键确认 → 发送给学生。

> 叙事口径（2026-08-11）：妙记因比赛租户权限不可用暂时搁置，转写能力用可插拔 ASR 层兜底
> （qwen_asr 已打通）。叙事重点从"依赖妙记"调整为"**语音进、决策出**"——
> AI 负责转写、清洗、评估脚手架，教师保留全部最终决定权，这与大赛"AI 准备，教师决定"
> 的红线一致；妙记作为权限恢复后的增强项出现在路线图里，不作为演示硬依赖。

一句话总结（备选 slogan）：

> 课堂语音从飞书进来，评估结果从飞书出去——AI 做转写与分析，教师做决定。

## 9. 与现有代码的改动清单

| 文件 | 改动 |
|------|------|
| `backend/requirements.txt` | 新增 `lark-oapi` |
| `backend/main.py` | 挂载 `feishu/routes.py` |
| `backend/database.py` | `StudentResponse` 增加 `feishu_minute_id`、`source`；新增 `FeishuBinding` 表（本地实体 ↔ 飞书记录映射） |
| `backend/schemas.py` | 新增音频导入、转写状态、同步状态相关模型 |
| `frontend/src/pages/GradingPage.jsx` | 作答来源标记（手动/妙记）；"同步到飞书"状态 |
| `frontend/src/pages/CommentsPage.jsx` | "发送给学生"按钮接通机器人推送 |
| `README.md` | 补充飞书集成章节 |
